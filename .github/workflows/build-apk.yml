name: Build RAL APK (Safe Mode)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  ANDROID_COMPILE_SDK: "36"
  ANDROID_BUILD_TOOLS: "36.0.0"
  ANDROID_NDK_VERSION: "28.0.13004108"
  ANDROID_CMAKE_VERSION: "3.22.1"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: write

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          lfs: true

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK Components
        run: |
          sdkmanager --install \
            "platforms;android-${ANDROID_COMPILE_SDK}" \
            "build-tools;${ANDROID_BUILD_TOOLS}" \
            "ndk;${ANDROID_NDK_VERSION}" \
            "cmake;${ANDROID_CMAKE_VERSION}"
          yes | sdkmanager --licenses || true

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # ==================== 安全版本 1: 开发者 (无资源压缩) ====================
      - name: Build Developer APK (Debug)
        run: |
          echo "🛠️ 构建开发者版本..."
          echo "✅ 无资源压缩，100% 资源完整"
          ./gradlew clean assembleDebug --no-daemon --stacktrace

      - name: Analyze Developer APK
        run: |
          DEBUG_APK="app/build/outputs/apk/debug/app-debug.apk"
          echo "📊 文件大小: $(du -h $DEBUG_APK | cut -f1)"
          echo "🔐 验证签名:"
          ${ANDROID_SDK_ROOT}/build-tools/${ANDROID_BUILD_TOOLS}/apksigner verify -v "$DEBUG_APK" || true

      - name: Upload Developer APK
        uses: actions/upload-artifact@v4
        with:
          name: RAL-Developer-Full
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
          if-no-files-found: error

      # ==================== 安全版本 2: 用户版 (保守优化) ====================
      - name: Generate Signing Key
        run: |
          keytool -genkey -v \
            -keystore debug.keystore \
            -alias androiddebugkey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Android Debug, O=Android, C=US"

      - name: Build Safe Release APK
        run: |
          echo "🚀 构建用户版本 (安全模式)..."
          echo "⚠️ 仅启用代码混淆，关闭资源压缩避免丢失"
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Sign & Analyze Release APK
        run: |
          UNSIGNED_APK="app/build/outputs/apk/release/app-release-unsigned.apk"
          SIGNED_APK="app/build/outputs/apk/release/app-release-safe.apk"
          
          if [ ! -f "$UNSIGNED_APK" ]; then
            UNSIGNED_APK=$(find app/build/outputs/apk/release -name "*-unsigned.apk" | head -1)
          fi
          
          [ -f "$UNSIGNED_APK" ] || { echo "❌ 未找到 APK"; exit 1; }
          
          ${ANDROID_SDK_ROOT}/build-tools/${ANDROID_BUILD_TOOLS}/apksigner sign \
            --ks debug.keystore \
            --ks-key-alias androiddebugkey \
            --ks-pass pass:android \
            --key-pass pass:android \
            --out "${SIGNED_APK}" \
            "${UNSIGNED_APK}"
          
          echo "📊 文件大小: $(du -h $SIGNED_APK | cut -f1)"
          ${ANDROID_SDK_ROOT}/build-tools/${ANDROID_BUILD_TOOLS}/apksigner verify -v "${SIGNED_APK}"
          rm -f "${UNSIGNED_APK}" "${UNSIGNED_APK}.idsig" debug.keystore

      - name: Upload Safe Release APK
        uses: actions/upload-artifact@v4
        with:
          name: RAL-User-Safe
          path: app/build/outputs/apk/release/app-release-safe.apk
          retention-days: 30
          if-no-files-found: error

      # ==================== 严格版本 3: 最大压缩 (风险版) ====================
      # 警告：此版本启用 shrinkResources，可能导致资源丢失！
      # 如需启用，取消下面的注释并确保已配置 keep.xml
      # 
      # - name: Build Strict Release APK (Risky)
      #   run: |
      #     echo "⚠️ 严格模式：启用资源压缩"
      #     ./gradlew assembleRelease -PstrictMode=true --no-daemon
      #   continue-on-error: true

      - name: Summary
        run: |
          echo "=========================================="
          echo "✅ 安全构建完成"
          echo "=========================================="
          echo ""
          echo "🛠️ 【开发者版 - 完整资源】"
          echo "   • 无资源压缩 (shrinkResources = false)"
          echo "   • 100% 资源完整性保证"
          echo "   • 适合日常开发调试"
          echo ""
          echo "🚀 【用户版 - 安全优化】"
          echo "   • 仅代码混淆 (minifyEnabled = true)"
          echo "   • ❌ 资源压缩已禁用 (避免丢失)"
          echo "   • 体积适中，安全性高"
          echo ""
          echo "⚠️  关于资源压缩 (shrinkResources):"
          echo "   如需启用，请先在 app/src/main/res/raw/keep.xml"
          echo "   中配置资源保留规则，否则可能导致运行时崩溃！"
          echo ""
          echo "📥 下载: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          echo "=========================================="
