# Rotating Art Launcher (RAL) 全自动APK构建工作流 - 许可+格式双修复版
name: RAL Full Auto Build APK

on:
  push:
    branches: [ main, master ]
    paths-ignore:
      - '**.md'
      - '**.txt'
      - 'LICENSE'
      - 'icons/**'
      - 'docs/**'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      build_type:
        description: '构建类型 (debug/release/all)'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release
          - all

env:
  ANDROID_COMPILE_SDK: "28"
  ANDROID_BUILD_TOOLS: "34.0.0"
  ANDROID_NDK_VERSION: "25.2.9519653"
  ANDROID_CMAKE_VERSION: "3.22.1"
  GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.parallel=true"

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      actions: write

    steps:
      # 1. 拉取代码（含子模块）
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      # 2. 项目结构校验
      - name: Validate Project Structure
        run: |
          echo "🔍 校验RAL项目核心文件..."
          [ ! -f "./gradlew" ] && echo "❌ 未找到gradlew文件" && exit 1
          [ ! -d "./app" ] && echo "❌ 未找到app模块目录" && exit 1
          echo "✅ 项目结构校验通过"

      # 3. 配置JDK 17
      - name: Set Up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      # 4. 核心修复：手动写入所有SDK许可证（彻底静默，无需交互）
      - name: Silent Accept All Android SDK Licenses
        run: |
          # 创建许可证目录（确保存在）
          mkdir -p $ANDROID_HOME/licenses
          # 写入所有必需的许可证签名（覆盖报错中提到的6类许可证）
          echo "24333f8a63b6825ea9c5514f83c2829b004d1fee" > $ANDROID_HOME/licenses/android-sdk-license
          echo "8933bad161af4178b1185d1a37fbf41ea5269c55" > $ANDROID_HOME/licenses/android-sdk-preview-license
          echo "d975f751698a77b662f1254ddbeed3901e976f5a" > $ANDROID_HOME/licenses/google-gdk-license
          echo "33b6a2b64607f11b759f320ef9dff4ae5c47d97a" > $ANDROID_HOME/licenses/mips-android-sysimage-license
          echo "186ba0d955324c9886e04c28c9e7563ab17bfd1" > $ANDROID_HOME/licenses/android-googletv-license
          echo "6f01893e7d88550537677a56445641fc8594f1a" > $ANDROID_HOME/licenses/android-googlexr-license
          # 验证许可证文件
          ls -l $ANDROID_HOME/licenses/
          echo "✅ 已静默接受所有Android SDK许可证"

      # 5. 修复packages格式：纯英文组件名+正确分隔符（无中文/注释）
      - name: Set Up Android Environment
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: "latest" # 使用预安装版本，避免冲突
          # 组件名纯英文+分号分隔，无中文/空格/注释
          packages: platforms;android-28;build-tools;34.0.0;ndk;25.2.9519653;cmake;3.22.1;platform-tools;extras;android;m2repository;extras;google;m2repository

      # 6. 配置Local Properties（指定SDK/NDK路径）
      - name: Configure Local Properties
        run: |
          echo "sdk.dir=${ANDROID_SDK_ROOT}" > local.properties
          echo "ndk.dir=${ANDROID_SDK_ROOT}/ndk/${ANDROID_NDK_VERSION}" >> local.properties
          cat local.properties
          echo "✅ Local Properties配置完成"

      # 7. Gradle依赖缓存优化
      - name: Cache Gradle Dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            .gradle/caches
            .gradle/wrapper
            app/build/cache
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 8. 构建环境准备（授权+清理）
      - name: Prepare Build Environment
        run: |
          chmod +x ./gradlew
          ./gradlew clean --quiet
          echo "✅ 构建环境准备完成"

      # 9. 构建Debug版APK（核心）
      - name: Build Debug APK
        if: github.event.inputs.build_type == 'debug' || github.event.inputs.build_type == 'all' || github.event_name != 'workflow_dispatch'
        id: build_debug
        continue-on-error: false
        run: |
          echo "🚀 开始构建Debug版APK..."
          ./gradlew assembleDebug --stacktrace
          # 验证APK生成
          DEBUG_APK=$(find app/build/outputs/apk/debug -name "*.apk" | head -n 1)
          [ -z "$DEBUG_APK" ] && echo "❌ Debug APK构建失败" && exit 1
          echo "✅ Debug APK构建完成：$DEBUG_APK"
          echo "debug_apk_path=$DEBUG_APK" >> $GITHUB_OUTPUT

      # 10. 构建Release版APK（可选）
      - name: Build Release APK (UnSigned)
        if: github.event.inputs.build_type == 'release' || github.event.inputs.build_type == 'all'
        id: build_release
        continue-on-error: true
        run: |
          echo "🚀 开始构建Release版APK（无签名）..."
          ./gradlew assembleRelease --stacktrace
          RELEASE_APK=$(find app/build/outputs/apk/release -name "*.apk" | head -n 1)
          [ -z "$RELEASE_APK" ] && echo "⚠️ Release APK构建失败（需配置签名）" && exit 0
          echo "✅ Release APK构建完成：$RELEASE_APK"
          echo "release_apk_path=$RELEASE_APK" >> $GITHUB_OUTPUT

      # 11. 上传构建产物
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: RAL-APKs-${{ github.sha }}-${{ github.event.inputs.build_type || 'debug' }}
          path: |
            app/build/outputs/apk/debug/*.apk
            app/build/outputs/apk/release/*.apk
          retention-days: 90
          if-no-files-found: warn

      # 12. 构建总结
      - name: Build Summary
        run: |
          echo "📊 构建总结："
          echo "- 触发方式：${{ github.event_name }}"
          echo "- 构建类型：${{ github.event.inputs.build_type || 'debug' }}"
          echo "- Debug APK：${{ steps.build_debug.outputs.debug_apk_path || '未构建' }}"
          echo "- Release APK：${{ steps.build_release.outputs.release_apk_path || '未构建/失败' }}"
          echo "✅ 工作流执行完成！"
