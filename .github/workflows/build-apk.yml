name: Build RAL APK (Debug & Safe & Strict)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  ANDROID_COMPILE_SDK: "36"
  ANDROID_BUILD_TOOLS: "36.0.0"
  ANDROID_NDK_VERSION: "28.0.13004108"
  ANDROID_CMAKE_VERSION: "3.22.1"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      actions: write

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          lfs: true

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK Components
        run: |
          sdkmanager --install \
            "platforms;android-${ANDROID_COMPILE_SDK}" \
            "build-tools;${ANDROID_BUILD_TOOLS}" \
            "ndk;${ANDROID_NDK_VERSION}" \
            "cmake;${ANDROID_CMAKE_VERSION}"
          yes | sdkmanager --licenses || true

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # ==================== 版本 1: Debug (无压缩) ====================
      - name: Build Developer APK (Debug)
        run: |
          echo "🛠️ 构建开发者版本..."
          ./gradlew clean assembleDebug --no-daemon

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: 1-RAL-Developer-Debug
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
          if-no-files-found: error

      # ==================== 版本 2: Safe Release (仅代码压缩) ====================
      - name: Generate Signing Key
        run: |
          keytool -genkey -v \
            -keystore debug.keystore \
            -alias androiddebugkey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Android Debug, O=Android, C=US"

      - name: Build Safe Release APK
        run: |
          echo "🚀 构建安全版本..."
          ./gradlew assembleRelease --no-daemon

      - name: Sign & Upload Safe APK
        run: |
          UNSIGNED="app/build/outputs/apk/release/app-release-unsigned.apk"
          [ ! -f "$UNSIGNED" ] && UNSIGNED=$(find app/build/outputs/apk/release -name "*-unsigned.apk" | head -1)
          [ -f "$UNSIGNED" ] || exit 1
          
          ${ANDROID_SDK_ROOT}/build-tools/${ANDROID_BUILD_TOOLS}/apksigner sign \
            --ks debug.keystore \
            --ks-key-alias androiddebugkey \
            --ks-pass pass:android \
            --key-pass pass:android \
            --out "app/build/outputs/apk/release/app-release-safe.apk" \
            "$UNSIGNED"
          rm -f "$UNSIGNED" "$UNSIGNED.idsig"

      - name: Upload Safe APK
        uses: actions/upload-artifact@v4
        with:
          name: 2-RAL-User-Safe
          path: app/build/outputs/apk/release/app-release-safe.apk
          retention-days: 30
          if-no-files-found: error

      # ==================== 版本 3: Strict Release (资源压缩) ====================
      - name: Enable Resource Shrinking
        run: |
          echo "⚠️  启用资源压缩 (shrinkResources)..."
          
          # 备份
          cp app/build.gradle app/build.gradle.bak
          
          # 方法1: 如果已存在 shrinkResources false，改为 true
          if grep -q "shrinkResources[[:space:]]*false" app/build.gradle; then
            sed -i 's/shrinkResources[[:space:]]*false/shrinkResources true/' app/build.gradle
            echo "✅ 已启用 shrinkResources (方法1: 替换false)"
          
          # 方法2: 在 release { 块中添加
          elif grep -q "minifyEnabled[[:space:]]*true" app/build.gradle; then
            # 在 minifyEnabled true 后面添加 shrinkResources true
            sed -i '/minifyEnabled[[:space:]]*true/a\            shrinkResources true' app/build.gradle
            echo "✅ 已添加 shrinkResources (方法2: 在minifyEnabled后)"
          
          # 方法3: 在 release { 后添加（通用）
          else
            # 找到 release { 并在下一行添加（缩进4空格）
            sed -i '/release[[:space:]]*{/a\            shrinkResources true' app/build.gradle
            echo "⚠️  已尝试添加 shrinkResources (方法3: 在release后)"
          fi
          
          # 验证
          if grep -q "shrinkResources[[:space:]]*true" app/build.gradle; then
            echo "✅ 验证成功：shrinkResources 已启用"
            grep -n "shrinkResources" app/build.gradle | head -2
          else
            echo "❌ 自动启用失败，请手动在 app/build.gradle 的 release 块中添加："
            echo "    shrinkResources true"
            exit 1
          fi

      - name: Build Strict Release APK
        id: build_strict
        run: |
          echo "🔥 构建严格版本（启用资源压缩）..."
          ./gradlew clean assembleRelease --no-daemon
        continue-on-error: true

      - name: Sign Strict APK
        id: sign_strict
        if: steps.build_strict.outcome == 'success'
        run: |
          UNSIGNED="app/build/outputs/apk/release/app-release-unsigned.apk"
          [ ! -f "$UNSIGNED" ] && UNSIGNED=$(find app/build/outputs/apk/release -name "*-unsigned.apk" | head -1)
          
          if [ -f "$UNSIGNED" ]; then
            ${ANDROID_SDK_ROOT}/build-tools/${ANDROID_BUILD_TOOLS}/apksigner sign \
              --ks debug.keystore \
              --ks-key-alias androiddebugkey \
              --ks-pass pass:android \
              --key-pass pass:android \
              --out "app/build/outputs/apk/release/app-release-strict.apk" \
              "$UNSIGNED"
            rm -f "$UNSIGNED" "$UNSIGNED.idsig"
            echo "success=true" >> $GITHUB_OUTPUT
          else
            echo "❌ 未找到 APK"
            echo "success=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload Strict APK
        if: steps.sign_strict.outputs.success == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: 3-RAL-User-Strict-TEST
          path: app/build/outputs/apk/release/app-release-strict.apk
          retention-days: 30

      - name: Restore build.gradle
        if: always()
        run: |
          [ -f "app/build.gradle.bak" ] && mv app/build.gradle.bak app/build.gradle
          rm -f debug.keystore
          echo "✅ 已清理"

      - name: Summary
        run: |
          echo "=========================================="
          echo "✅ 构建完成"
          echo "=========================================="
          echo ""
          echo "📦 1-RAL-Developer-Debug    : 无压缩，可调试"
          echo "📦 2-RAL-User-Safe          : 仅代码压缩（推荐测试）"
          echo "📦 3-RAL-User-Strict-TEST   : 代码+资源压缩（需详细测试）"
          echo ""
          echo "⚠️  Strict 版本必须测试："
          echo "   - 所有图片是否正常显示"
          echo "   - 反射加载的资源是否存在"
          echo "   - 第三方SDK是否工作正常"
          echo "=========================================="
