name: Tim Loi Android 7

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 1. TIM LOI NOTIFICATION CHANNEL
        run: grep -rn "NotificationChannel" . --include="*.kt" --include="*.java" || echo "Khong co"

      - name: 2. TIM LOI FOREGROUND SERVICE
        run: grep -rn "startForegroundService" . --include="*.kt" --include="*.java" || echo "Khong co"

      - name: 3. TIM LOI DISPLAY CUTOUT (TAI THO)
        run: grep -rn "DisplayCutout" . --include="*.kt" --include="*.java" || echo "Khong co"

      - name: 4. DOC FILE INITIALIZATION (NOI GAY CRASH)
        run: cat ./app/src/main/java/com/app/ralaunch/feature/init/InitializationActivity.kt || echo "Khong tim thay file nay"

      - name: 5. DOC FILE MAIN ACTIVITY (NOI GAY CRASH)
        run: cat ./app/src/main/java/com/app/ralaunch/feature/main/MainActivityCompose.kt || echo "Khong tim thay file nay"
