<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/game_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- 主游戏内容区域的外层容器 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false">
        
        <!-- 游戏内容区域（SDLSurface + ControlLayout） -->
        <FrameLayout
            android:id="@+id/game_content_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- 游戏控制布局会动态添加到这里 -->

        </FrameLayout>
        
        <!-- 悬浮UI层 (确保在游戏内容之上) -->
        <FrameLayout
            android:id="@+id/game_overlay_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="false"
            android:focusable="false">

            <!-- 可拖动的 MD3 风格悬浮菜单按钮（半透明，与外部编辑器一致） -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/game_drawer_button"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="top|center_horizontal"
                android:layout_marginTop="16dp"
                app:cardCornerRadius="28dp"
                app:cardElevation="8dp"
                app:cardBackgroundColor="#CCF3EDF7"
                app:strokeWidth="0dp"
                android:alpha="0.85"
                android:clickable="true"
                android:focusable="true"
                android:contentDescription="设置"
                android:visibility="gone">
                
                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_settings"
                    app:tint="?attr/colorPrimary" />
                    
            </com.google.android.material.card.MaterialCardView>

            <!-- 可拖动的 MD3 风格悬浮编辑器设置按钮（半透明） -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/game_editor_settings_button"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="top|end"
                android:layout_marginTop="16dp"
                android:layout_marginEnd="16dp"
                app:cardCornerRadius="28dp"
                app:cardElevation="8dp"
                app:cardBackgroundColor="#CCF3EDF7"
                app:strokeWidth="0dp"
                android:alpha="0.85"
                android:clickable="true"
                android:focusable="true"
                android:contentDescription="编辑设置"
                android:visibility="gone">
                
                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_settings"
                    app:tint="?attr/colorOnPrimaryContainer" />
                    
            </com.google.android.material.card.MaterialCardView>

        </FrameLayout>
        
    </RelativeLayout>

    <!-- 右侧抽屉菜单（MD3 风格，半透明） -->
    <ListView
        android:id="@+id/game_navigation_view"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="#E6FFFFFF"
        android:alpha="0.90"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:fitsSystemWindows="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:paddingStart="0dp"
        android:paddingEnd="0dp"
        android:clipToPadding="false" />

</androidx.drawerlayout.widget.DrawerLayout>

