<?xml version="1.0" encoding="utf-8"?>
<!-- 主界面 - GitHub/Kotlin 现代化风格 (Material Design 3 + NavigationRail + TopAppBar) -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootFrameLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- 背景图片视图 -->
    <ImageView
        android:id="@+id/backgroundImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:importantForAccessibility="no" />

    <!-- 视频背景视图 -->
    <com.app.ralaunch.view.VideoBackgroundView
        android:id="@+id/videoBackgroundView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:visibility="gone" />

    <!-- 前景半透明遮罩 -->
    <View
        android:id="@+id/foregroundOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:visibility="gone" />

    <!-- MD3 Scaffold 风格主布局 -->
    <LinearLayout
        android:id="@+id/mainLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:alpha="1.0"
        android:background="@android:color/transparent">

        <!-- TopAppBar - Material 3 现代化风格（所有页面统一隐藏） -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@android:color/transparent"
            android:elevation="0dp"
            android:visibility="gone"
            app:title="Rotating art Launcher"
            app:titleTextColor="?attr/colorOnSurface"
            app:titleTextAppearance="@style/TextAppearance.Material3.TitleMedium" />

        <!-- 内容区域 - NavigationRail + Content -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:background="@android:color/transparent">

            <!-- 左侧 NavigationRail - 现代化风格 (80dp) -->
            <com.google.android.material.navigationrail.NavigationRailView
                android:id="@+id/navigationRail"
                android:layout_width="80dp"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                app:itemIconTint="@color/navigation_rail_item_color"
                app:itemTextColor="@color/navigation_rail_item_color"
                app:itemActiveIndicatorStyle="@style/Widget.App.NavigationRailView.ActiveIndicator"
                app:labelVisibilityMode="labeled"
                app:menu="@menu/navigation_rail_menu" />

            <!-- 主内容区域 - 根据导航切换 -->
            <FrameLayout
                android:id="@+id/contentContainer"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@android:color/transparent">

                <!-- 游戏列表页面 -->
                <LinearLayout
                    android:id="@+id/gameListPage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="horizontal"
                    android:background="@android:color/transparent"
                    android:padding="16dp"
                    android:visibility="visible">

                    <!-- 游戏列表区域 (75%) -->
                    <com.google.android.material.card.MaterialCardView
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="3"
                        android:layout_marginEnd="12dp"
                        app:cardBackgroundColor="?attr/colorSurface"
                        app:strokeWidth="1dp"
                        app:strokeColor="?attr/textSecondary"
                        style="@style/Widget.App.CardView.Standard">

                        <FrameLayout
                            android:layout_width="match_parent"
                            android:layout_height="match_parent">

                            <!-- 游戏 RecyclerView -->
                            <androidx.recyclerview.widget.RecyclerView
                                android:id="@+id/gameRecyclerView"
                                android:layout_width="match_parent"
                                android:layout_height="match_parent"
                                android:clipChildren="false"
                                android:clipToPadding="false"
                                android:overScrollMode="never"
                                android:padding="12dp"
                                android:scrollbars="vertical"
                                app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
                                app:spanCount="2" />

                        </FrameLayout>

                    </com.google.android.material.card.MaterialCardView>

                    <!-- 右侧信息区域 (25%) -->
                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:orientation="vertical">

                        <!-- 游戏信息区域 -->
                        <ScrollView
                            android:layout_width="match_parent"
                            android:layout_height="0dp"
                            android:layout_weight="1"
                            android:fillViewport="true"
                            android:paddingBottom="8dp">

                            <LinearLayout
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:orientation="vertical">

                                <!-- 空状态提示 -->
                                <LinearLayout
                                    android:id="@+id/emptySelectionText"
                                    android:layout_width="match_parent"
                                    android:layout_height="wrap_content"
                                    android:orientation="vertical"
                                    android:gravity="center"
                                    android:padding="32dp"
                                    android:visibility="visible">

                                    <ImageView
                                        android:layout_width="64dp"
                                        android:layout_height="64dp"
                                        android:src="@drawable/ic_game_controller"
                                        app:tint="?attr/colorOnSurfaceVariant"
                                        android:alpha="0.4"
                                        android:layout_marginBottom="16dp" />

                                    <TextView
                                        android:layout_width="wrap_content"
                                        android:layout_height="wrap_content"
                                        android:text="@string/main_select_game_hint"
                                        android:textColor="?attr/textSecondary"
                                        android:textSize="16sp"
                                        android:textStyle="bold" />

                                    <TextView
                                        android:layout_width="wrap_content"
                                        android:layout_height="wrap_content"
                                        android:layout_marginTop="8dp"
                                        android:text="@string/main_select_game_instruction"
                                        android:textColor="?attr/textHint"
                                        android:textSize="13sp" />

                                </LinearLayout>

                                <!-- 选中的游戏信息卡片 -->
                                <com.google.android.material.card.MaterialCardView
                                    android:id="@+id/selectedGameInfo"
                                    android:layout_width="match_parent"
                                    android:layout_height="wrap_content"
                                    android:layout_marginTop="8dp"
                                    android:visibility="gone"
                                    app:cardBackgroundColor="?attr/colorSurface"
                                    app:strokeWidth="1dp"
                                    app:strokeColor="?attr/colorOutline"
                                    style="@style/Widget.App.CardView.Standard">

                                    <LinearLayout
                                        android:layout_width="match_parent"
                                        android:layout_height="wrap_content"
                                        android:orientation="vertical"
                                        android:padding="20dp"
                                        android:gravity="center">

                                        <!-- 游戏图标容器 -->
                                        <LinearLayout
                                            android:layout_width="100dp"
                                            android:layout_height="100dp"
                                            android:background="@drawable/bg_rounded_transparent"
                                            android:backgroundTint="?attr/colorSurface"
                                            android:gravity="center"
                                            android:padding="12dp">

                                            <ImageView
                                                android:id="@+id/selectedGameImage"
                                                android:layout_width="match_parent"
                                                android:layout_height="match_parent"
                                                android:scaleType="centerCrop"
                                                android:src="@drawable/ic_game_default" />

                                        </LinearLayout>

                                        <!-- 游戏名称 -->
                                        <TextView
                                            android:id="@+id/selectedGameName"
                                            android:layout_width="match_parent"
                                            android:layout_height="wrap_content"
                                            android:layout_marginTop="16dp"
                                            android:gravity="center"
                                            android:text="@string/game_name"
                                            android:textSize="18sp"
                                            android:textStyle="bold"
                                            android:maxLines="1"
                                            android:ellipsize="end"
                                            android:textColor="?attr/textPrimary" />

                                        <!-- 游戏描述 -->
                                        <TextView
                                            android:id="@+id/selectedGameDescription"
                                            android:layout_width="match_parent"
                                            android:layout_height="wrap_content"
                                            android:layout_marginTop="8dp"
                                            android:gravity="center"
                                            android:text="@string/game_description"
                                            android:maxLines="2"
                                            android:ellipsize="end"
                                            android:textColor="?attr/textSecondary"
                                            android:textSize="13sp" />


                                    </LinearLayout>

                                </com.google.android.material.card.MaterialCardView>

                            </LinearLayout>

                        </ScrollView>

                        <!-- 启动游戏按钮 -->
                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/launchGameButton"
                            android:layout_width="match_parent"
                            android:layout_height="56dp"
                            android:layout_marginTop="8dp"
                            android:layout_marginBottom="16dp"
                            android:text="@string/main_launch_game"
                            android:textSize="16sp"
                            android:textStyle="bold"
                            app:icon="@drawable/ic_play"
                            app:iconGravity="textStart"
                            app:iconPadding="8dp"
                            app:iconSize="20dp"
                            app:cornerRadius="12dp"
                            style="@style/Widget.Material3.Button.TonalButton"
                            android:visibility="gone" />

                    </LinearLayout>

                </LinearLayout>

                <!-- 控制布局页面 -->
                <FrameLayout
                    android:id="@+id/controlPage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:visibility="gone" />

                <!-- 下载页面 -->
                <FrameLayout
                    android:id="@+id/downloadPage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:visibility="gone" />

                <!-- 设置页面 -->
                <FrameLayout
                    android:id="@+id/settingsPage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:visibility="gone" />

                <!-- 导入游戏页面 -->
                <FrameLayout
                    android:id="@+id/importPage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:visibility="gone" />

                <!-- 控件商店页面 -->
                <FrameLayout
                    android:id="@+id/controlStorePage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:visibility="gone" />

            </FrameLayout>

        </LinearLayout>

    </LinearLayout>

    <!-- Fragment 全屏容器 - 用于初始化、设置等全屏页面 -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:background="@android:color/transparent"
        android:elevation="8dp" />

</FrameLayout>
