<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="?attr/backgroundSecondary"
    app:cardCornerRadius="8dp"
    app:cardElevation="1dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/textSecondary">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <!-- 组件信息区域 -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- 组件名称 -->
            <TextView
                android:id="@+id/componentName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/component_name_placeholder"
                android:textColor="?attr/textPrimary"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginBottom="2dp" />

            <!-- 组件描述 -->
            <TextView
                android:id="@+id/componentDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/component_description_placeholder"
                android:textColor="?attr/textSecondary"
                android:textSize="12sp" />

        </LinearLayout>

        <!-- MD3 圆形进度指示器 -->
        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progressBar"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="12dp"
            android:indeterminate="false"
            android:visibility="visible"
            app:indicatorSize="28dp"
            app:trackThickness="2.5dp"
            app:indicatorColor="?attr/colorPrimary"
            app:trackColor="?attr/backgroundSecondary"
            android:contentDescription="@string/content_desc_component_progress" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
